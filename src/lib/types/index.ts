/**
 * Re-export types from the original location
 * This allows gradual migration to new structure while maintaining compatibility
 */

// Re-export electron types
export * from '../../types/electron.d';
